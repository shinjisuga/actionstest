name: test
on:
    pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test1
        run: echo '${{ toJSON(github) }}'
      - name: test2
        run: echo '${{ toJSON(github.event.user) }}'
      - name: test3
        run: echo '${{ github.event.user.login }}'
      - name: test4
        run: echo '${{ toJSON(github.event.base.user) }}'
      - name: test5
        run: echo '${{ github.event.base.user.login }}'